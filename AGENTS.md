# AGENTS.md

## Project
- Node.js TypeScript project for an OpenCode mTLS proxy.

## Commands
- Build: `npm run build`
- Test: `npm test`

## Guidelines
- Keep changes minimal and focused.
- Prefer existing patterns in `src/` and `tests/`.
- Do not commit generated `dist/` changes unless explicitly requested.
